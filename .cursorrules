# ai-comic-generator — Source Control「Generate commit message」(✨) 专用

当为本仓库**自动生成 Git 提交说明**（SCM 输入框 sparkle）时，必须遵守：

## 格式（单行 header，尽量不加 body）

```text
<type>(<scope>): <subject>
```

- **subject 必须使用简体中文**（禁止英文描述），动词开头，句末不加句号。
- 整行 header **不超过 100 个字符**。
- **scope 必填**，且只能选一个（见下表）。

## type（仅允许）

`feat` | `fix` | `docs` | `style` | `refactor` | `perf` | `test` | `chore` | `ci` | `build` | `revert`

## scope（按本次 staged 变更路径固定选取）

| staged 主要路径 | scope |
|-----------------|-------|
| 仅 `web/` | `web` |
| 仅 `server/` | `server` |
| 仅 `docs/`、`.cursor/skills/`、`.cursor/rules/`（文档/规范类） | `docs` |
| 仅 CI、husky、`.github/`、`commitlint` 等 | `ci` |
| 根目录依赖/锁文件/构建脚本，且无明显 web/server 功能改动 | `chore` |
| 同一功能同时改 `web/` + `server/` | 以**主交付**为准：后端 API/模型为主 → `server`；仅前端对接/UI → `web`；难以判断 → `chore` |

## 示例

```text
feat(server): 添加管理端收款记录分页与汇总接口
feat(web): 新增充值页并对接支付 API
fix(web): 修复登录过期未跳转
docs(docs): 补充充值技术方案说明
chore(ci): 调整 commitlint 规则
```

勿提交 `config.yaml`、`.env`、生成的 `web/src/components.d.ts` / `auto-imports.d.ts` 等；若 diff 含这些，subject 仍只描述 intentional 的功能变更。
