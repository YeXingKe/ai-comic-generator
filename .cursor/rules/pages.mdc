---
description: web/src/pages 页面组件约定
globs: web/src/pages/**/*.tsx
alwaysApply: false
---

# 页面（`web/src/pages`）

## 目录职责

| 路径 | 面向 | 示例 |
|------|------|------|
| `common/` | 公开 | `home`、`auth` |
| `user/` | 登录用户 | `center`、`create`、`history`、`article/detail` |
| `admin/` | 管理员 | `Users`、`StaticPage` |
| `_shared/` | 共享样式/工具 | `pageShell.scss`、`userTableHelpers.ts` |

勿在 `user/center` 做管理员 CRUD。勿把个人资料与用户列表混在同一页。

## 文件组织

- 每功能：`index.tsx` + `index.scss`
- 引入共享布局：`@import '@/pages/_shared/pageShell.scss';`
- 类名：`page-shell`、`page-shell__header`、`page-shell__table`

## 组件放置边界（重要）

`pages/` 只放**页面层**代码：页面壳、布局、数据编排（取数 / 状态 / 把数据分发给子组件）、页面级样式。

**可复用 / 可封装的 UI 组件一律放 `web/src/components/<组件族>/`**，不要在页面目录下建 `components/`、`charts/` 等子目录。

| 该放哪 | 内容 |
|--------|------|
| `pages/xxx/index.tsx` | 页面壳、栅格布局、`useState/useEffect` 取数、组装子组件 |
| `pages/xxx/index.scss` | 仅页面骨架与栅格样式 |
| `components/StatCharts/` | 图表族、KPI/图表卡等封装组件 + 自带样式 `index.css` |
| `constants/` | mock / 静态数据（如 `statMock.ts`、`mockArticles.ts`） |
| `api/` + `types/api.ts` | 请求与类型 |

- 组件族用 `index.ts` 统一 re-export，页面按 `import { A, B } from '@/components/StatCharts'` 引入。
- 封装组件自带样式写在组件族目录内（如 `components/StatCharts/index.css`），由 `index.ts` 引入，勿把组件样式散落到页面 `index.scss`。
- 判断标准：组件是否只服务于这一个页面且不含复用意图？是 → 可留页面内单文件；只要有第二处复用可能、或属于「图表 / 表单 / 卡片」这类通用族 → 移到 `components/`。

## UI 模式

- 列表 → Ant Design `Table` + 分页（不用 `AppleCardList`）
- 管理端表格列抽到同目录 `*TableColumns.tsx`
- 未登录：用 `Empty` 跳转 `/user/login`，或依赖 `RequireAuth` 路由守卫

## 数据

- 示例文章：`@/constants/mockArticles`
- 用户 API：`@/api/user.ts`，类型在 `@/types/api.ts`
- 编辑后由 hook 自动 Prettier 格式化，勿为 antd 手写 import
